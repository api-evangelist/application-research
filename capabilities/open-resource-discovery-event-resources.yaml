naftiko: 1.0.0-alpha2
info:
  label: Application Research Open Resource Discovery (ORD) API — Event Resources
  description: 'Application Research Open Resource Discovery (ORD) API — Event Resources. 2 operations. Lead operation: Application
    Research List all Event Resources. Self-contained Naftiko capability covering one Application Research business surface.'
  tags:
  - Application Research
  - Event Resources
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    APPLICATION_RESEARCH_API_KEY: APPLICATION_RESEARCH_API_KEY
capability:
  consumes:
  - type: http
    namespace: open-resource-discovery-event-resources
    baseUri: https://api.example.com/ord/v1
    description: Application Research Open Resource Discovery (ORD) API — Event Resources business capability. Self-contained,
      no shared references.
    resources:
    - name: event-resources
      path: /event-resources
      operations:
      - name: listeventresources
        method: GET
        description: Application Research List all Event Resources
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: event-resources-ordId
      path: /event-resources/{ordId}
      operations:
      - name: geteventresource
        method: GET
        description: Application Research Get an Event Resource by ORD ID
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.APPLICATION_RESEARCH_API_KEY}}'
  exposes:
  - type: rest
    namespace: open-resource-discovery-event-resources-rest
    port: 8080
    description: REST adapter for Application Research Open Resource Discovery (ORD) API — Event Resources. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/event-resources
      name: event-resources
      description: REST surface for event-resources.
      operations:
      - method: GET
        name: listeventresources
        description: Application Research List all Event Resources
        call: open-resource-discovery-event-resources.listeventresources
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/event-resources/{ordid}
      name: event-resources-ordid
      description: REST surface for event-resources-ordId.
      operations:
      - method: GET
        name: geteventresource
        description: Application Research Get an Event Resource by ORD ID
        call: open-resource-discovery-event-resources.geteventresource
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: open-resource-discovery-event-resources-mcp
    port: 9090
    transport: http
    description: MCP adapter for Application Research Open Resource Discovery (ORD) API — Event Resources. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: application-research-list-all-event
      description: Application Research List all Event Resources
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: open-resource-discovery-event-resources.listeventresources
      outputParameters:
      - type: object
        mapping: $.
    - name: application-research-get-event-resource
      description: Application Research Get an Event Resource by ORD ID
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: open-resource-discovery-event-resources.geteventresource
      outputParameters:
      - type: object
        mapping: $.
